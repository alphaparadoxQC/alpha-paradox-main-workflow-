import { describe, it, expect } from 'vitest';
import { pureStateToDensityMatrix, applySingleQubitChannel, applyTwoQubitChannel } from '../densityMatrix';
import { getGateMatrix } from '../gates';

describe('Density Matrix Simulator', () => {
  it('Should have tests soon', () => {
    expect(true).toBe(true);
  });
});
