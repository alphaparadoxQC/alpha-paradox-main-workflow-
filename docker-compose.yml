version: '3.8'

services:
  backend:
    build:
      context: ./chemistry-backend
    ports:
      - "8000:8000"
    volumes:
      - ./chemistry-backend:/app
    environment:
      - PYTHONUNBUFFERED=1
